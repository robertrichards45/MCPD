import type { PacketPreview } from '../buildPacket';

export type PacketReviewPageProps = {
  packet: PacketPreview;
};

export function PacketReviewPage(_props: PacketReviewPageProps): null {
  return null;
}
