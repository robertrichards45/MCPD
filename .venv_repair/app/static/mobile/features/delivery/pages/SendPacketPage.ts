import type { PacketPreview } from '../buildPacket';

export type SendPacketPageProps = {
  packet: PacketPreview;
  recipient: string;
  ccList: string[];
};

export function SendPacketPage(_props: SendPacketPageProps): null {
  return null;
}
